---
"@cordn/cli": patch
---

Document the programmatic (library) integration path in `cordn docs agent`: `openPersistentSession` embedding for Node agent harnesses, with lock/durability semantics, alongside the existing filesystem-queue flow.
